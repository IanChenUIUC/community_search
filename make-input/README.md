## About

A simple script that downloads the networks and converts them to the canonical edgelist format.
The canonical edgelist format has sorted and contiguous node IDs (0, 1, .., n-1), a header `source,target`, and no self-loops, multi-edges, and is undirected.
