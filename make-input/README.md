## About

A simple script that downloads the networks and converts them to the canonical edgelist format.

TODO: add the files for cen and abm networks (from Illinois Databank, whenever that is updated...)
