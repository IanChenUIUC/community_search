[project]
name = "abm272-scaling"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click",
    "format-conversion",
    "numpy",
    "polars",
    "pybind11>=2.12",
]

[tool.uv.sources]
format-conversion = { git = "https://github.com/IanChenUIUC/format-conversion.git" }

[tool.ruff]
line-length = 100
